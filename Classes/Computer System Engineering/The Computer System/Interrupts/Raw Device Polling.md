The CPU periodically checks device for requests. If there are no requests, then it will return to resume the [[Application (User) Program]].

This is **NOT SUITABLE** for devices with irregular I/O usage