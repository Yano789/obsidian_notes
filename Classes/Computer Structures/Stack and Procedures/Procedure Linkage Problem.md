We need to establish a system where:
- the [[Function]] can always access its **arguments/parameters**
- the [[Function]] is expected to return a value (*if any*) in a **known memory location/address** so we can access it (delete local variables)
- The CPU can find the address of the caller