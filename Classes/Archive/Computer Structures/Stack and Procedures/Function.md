Must have these characteristics:
- a callable, **reusable** series of instructions
- has a single **named entry point** - we know the address of the first instruction to this function
- is **parametrizable** - can have different arguments
- has **local variables**, which are lost after the function is finished